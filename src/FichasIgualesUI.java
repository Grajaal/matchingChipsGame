import java.awt.Component;
import java.util.ArrayList;
import java.util.Scanner;

import javax.swing.JFrame;
import javax.swing.JOptionPane;
import javax.swing.JPanel;
import javax.swing.JRootPane;

public class FichasIgualesUI {
    public Tablero init(GUI gui) {
        // JRootPane rootPane = gui.getRootPane();
        // JFrame frame = (JFrame) rootPane.getContentPane();
        // Component component = frame.getComponent(0);
        // if (component instanceof JPanel) {

        // } else {
        // JOptionPane.showMessageDialog(null, "Primero carga un juego.");
        // }

        return null;
    }
}
